# Szablon zmiennych środowiskowych.
# Skopiuj ten plik do .env (który jest ignorowany przez Git) i uzupełnij prawdziwe wartości.

# Konfiguracja Bazy Danych
# Użytkownik, hasło i nazwa bazy danych używane zarówno przez kontener PostgreSQL,
# jak i przez aplikację Go do budowy connection stringa.
POSTGRES_USER=fileserver
POSTGRES_PASSWORD=your_super_secret_password_here
POSTGRES_DB=fileserver_db

# Konfiguracja Aplikacji
# Długi, losowy ciąg znaków używany do podpisywania tokenów JWT.
JWT_SECRET=your_super_long_and_random_jwt_secret_here